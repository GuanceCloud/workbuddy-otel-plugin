{
  "name": "workbuddy-otel-plugin",
  "version": "0.1.4",
  "description": "GTrace-compatible OpenTelemetry traces and metrics for WorkBuddy.",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "node scripts/check.js",
    "test": "node --test test/*.test.js",
    "build:release": "bash scripts/build-release.sh"
  },
  "engines": {
    "node": ">=22"
  }
}
